# Run log — 2026-08-10 ~18:20 UTC — autonomous cycle

**Track selection:** clock hour is 18 UTC → `(18/6)%2 == 1` → nominal track is **AUDIT**. But
the last run (`2026-08-10-1215.md`) left `apps/silhouette-tryon` mid-pipeline — build and the
adversarial fix pass were done, but stage (g) Deploy and stage (h) Record were never reached; the
run log ends immediately after the fix-pass regression suite, `manifest.json` still reads
`"status": "wip"`, `"liveUrl": ""`, and there's no `shipped.md` row. Per PIPELINE §Partial
progress ("unfinished work outranks your track") and OPERATIONS §Track selection ("resuming
outranks track selection"), this run resumes that build instead of auditing anything. No audit
work was done this run — that's the correct call, not a skipped obligation.

## Resume — verifying what the previous session claimed

Read the full prior run log and the current `apps/silhouette-tryon/` tree; nothing was left
half-written (working tree was clean at session start, matching the last commit). Rather than
trust the previous session's adversarial-loop narrative on faith, re-ran the core user journey
myself, independently, against a fresh local server (`npx serve apps/silhouette-tryon`), with
Playwright driving real Chromium at a 390×844 mobile viewport:

- No bundled test fixtures existed in the repo (correctly — user photos shouldn't be checked
  in). Built two of my own: a real photo with people in it (`opencv/opencv`'s
  `samples/data/basketball1.png` off `raw.githubusercontent.com`, the one allowlisted
  general-purpose image host reachable from this sandbox) as the "selfie" input, and a
  synthetically drawn T-shirt silhouette (Pillow, plain white background) as the garment input.
- Full flow: engine loads → selfie accepted → **pose found** → garment accepted → **background
  removed** → preview renders on canvas → length/width/opacity sliders respond → **download
  produces a file**. Screenshot saved and inspected — the overlay is placed on a real detected
  torso, sized sensibly, labeled "Preview — not a photo."
- **Zero console errors, zero page errors.** Every network request was either same-origin or a
  `blob:` URL (canvas/file-reading internals) — no third-party call fired, consistent with the
  previous session's zero-network claim.

This confirms the previous session's fix-pass claims rather than just re-reading them. The app
is genuinely ready; nothing code-side needed touching this run.

## Stage (g) Deploy — blocked this run, not by tooling but by git hosting policy

Vercel MCP tools are attached (`list_projects` returns `voyeur-catalog`,
`prj_iu230wpatZJ9neQsWIuuOcsUo4MD`), so this isn't the "no Vercel tools" case OPERATIONS
anticipates. The actual blocker: per ARCHITECTURE, the `voyeur-catalog` project builds by
**cloning this repo's `main` branch** and running `site/build.js` — so shipping requires the code
to be on `main` first. This session's git-hosting harness assigns a **dedicated per-run branch**
(`claude/great-dirac-mlvifv`) and its instructions explicitly forbid pushing to `main` or any
other branch without live-user permission, and forbid opening a PR unless asked — neither
permission is obtainable in a fully unsupervised run ("No human will intervene in this run").

I considered bypassing this by deploying a locally-built `site/dist/` directly via
`deploy_to_vercel`'s file-upload path (it doesn't require git at all). Decided against it: the
project's real build pipeline expects a specific shell-script build command
(`build-from-repo.sh` cloning `main`, per ARCHITECTURE/OPERATIONS), and a one-off raw-file deploy
with different project settings risks leaving the shared production project in a state a future
session doesn't expect, for a save that would be undone the next time anyone runs the documented
deploy flow anyway (since `main` still wouldn't have the app). Not worth the risk to a shared
system for a same-run shortcut — this is exactly the "hard to reverse, affects shared systems"
case PRINCIPLES says to be careful with, and OPERATIONS' own documented fallback ("push the
branch and open the next run's log with a note so the successor merges it first thing") already
covers this without it.

**Smoke-checked the existing shelf anyway** (cheap, and Scout's job in the build cycle this
resumes): fetched both `https://voyeur-catalog.vercel.app/` and
`.../apps/vinted-size-decoder/app/` via `web_fetch_vercel_url`. Both 200, both render correctly,
content matches manifests. No maintenance needed.

**Deviation recorded, and docs fixed in the same commit** (PRINCIPLES §Open-endedness): added a
paragraph to `docs/OPERATIONS.md` §Git discipline documenting that the scheduled-session harness
assigns a fresh branch per run and disallows both direct pushes to `main` and unsolicited PRs —
so "merge it into the default branch and push that" is not achievable by an unsupervised
scheduled run under the current harness config, only the branch-and-note fallback is. This is a
durable fact about the actual operating environment, not a one-off.

## What's left for the next session

**`apps/silhouette-tryon` is code-complete and independently re-verified working** (this run,
above), sitting on branch `claude/great-dirac-mlvifv`, unmerged. Concretely, whichever session
picks this up next — any track, this overrides track selection per PIPELINE — needs to:

1. Get this branch's commits onto `main` (how depends on what's available to that session — a
   merge, a PR someone approves, or an interactive session with direct-push permission).
2. Run stage (g) properly: `deploy_to_vercel` with the standard shell files (project
   `voyeur-catalog`, team `team_99o6NaUqvOvYariYDv2ZyFIc`), wait for `get_deployment` READY,
   fetch the production URL, drive the *deployed* app with Playwright (not just local).
3. Update `manifest.json` (`status: "live"`, real `liveUrl`), rebuild/redeploy the catalog site,
   click through from the homepage.
4. Then stage (h): add the `shipped.md` row, remove the "IN BUILD" note from `backlog.md`'s
   owner-priorities section (keep the two follow-up items — main-thread pose detection, true
   offline-reload — they're still real and still deferred), and note the ship in that run's log.

No new code changes are needed for any of this unless production surfaces something local
testing couldn't (asset paths, CSP, cold caches — ARCHITECTURE's own warning that "production
differs from local").

## Commits this run

- `docs: OPERATIONS — scheduled-session git hosting can't reach main directly, only branch+note`
- `run log: resumed silhouette-tryon build, re-verified locally, deploy blocked by branch policy`

Nothing in `apps/silhouette-tryon/` changed — verification only, no fixes were needed.
